/*
 * Escribe un programa que muestre en pantalla los números del 20 al 10 (incluidos los dos) con una línea vacía entre ellos.
 * Pistas:
 * 1. Si definimos bien la condición de un bucle for, podemos hacer que el valor de i se corresponda con cada uno de los números que queremos mostrar. 
 * 2. El método WriteLine muestra por consola el mensaje que le pasamos entre paréntesis y a continuación hace un salto de línea, 
 *     con lo que lo siguiente que mostramos aparece en la línea de abajo. Claro que si no le pasamos ningún mensaje sólo hace el salto de línea.
 */
using System;

namespace _21_Ejercicio_Repaso_03
{
    class Program
    {
        static void Main(string[] args)
        {
            //Introduce aquí tu código
        }
    }
}
